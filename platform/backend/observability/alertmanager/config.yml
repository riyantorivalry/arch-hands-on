global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: 'default'
    # No-op receiver; Add webhook/email/slack here for real notifications
    webhook_configs:
      - url: 'http://localhost:9094/'

